#include "QImPlot3DAxisTransform.h"

namespace QIM
{

/**
 * \if ENGLISH
 * @brief Destructor for QImPlot3DAxisTransform
 * @details Default destructor — lifetime is managed externally by the user.
 * \endif
 *
 * \if CHINESE
 * @brief QImPlot3DAxisTransform 析构函数
 * @details 默认析构函数 — 生命周期由用户外部管理。
 * \endif
 */

// QImPlot3DAxisTransform is a pure interface class with no data members.
// The virtual destructor and pure virtual methods are defined inline in the header.
// This .cpp file exists for future expansion (e.g., common utility methods)
// and to satisfy the GLOB-based CMake build system.

}  // namespace QIM